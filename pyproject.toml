[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ffl-history"
version = "1.0.0"
description = "Export an ESPN fantasy football league's full history to SQLite, Postgres, CSV, Parquet and more"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["espn", "fantasy-football", "etl", "sqlite", "postgres", "parquet"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Topic :: Database",
    "Topic :: Games/Entertainment",
]
dependencies = [
    "espn-api>=0.44,<1",
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
postgres = ["psycopg[binary]>=3.1"]
parquet  = ["pyarrow>=15"]
excel    = ["openpyxl>=3.1"]
duckdb   = ["duckdb>=0.10"]
all      = ["ffl-history[postgres,parquet,excel,duckdb]"]
dev      = ["ffl-history[all]", "pytest>=8", "ruff>=0.6"]

[project.scripts]
ffl-history = "ffl_history.cli:main"

[project.urls]
Homepage = "https://github.com/OWNER/ffl-history"
Issues = "https://github.com/OWNER/ffl-history/issues"

[tool.setuptools.packages.find]
include = ["ffl_history*"]

[tool.setuptools.package-data]
ffl_history = ["schema/*.sql", "env.example"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]
